<%= form_for(woman) do |f| %>
  <% if woman.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(woman.errors.count, "error") %> prohibited this woman from being saved:</h2>

      <ul>
      <% woman.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.select:product_id, Product.all.order(:id).collect{|s| [( s.name), s.id]}%> 
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
